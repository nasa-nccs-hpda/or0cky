# Status slide deck — filesystem record

Filesystem copy of the "ROCKE-3D → JAX: Status" slide deck (2026-09-21),
kept alongside `STATUS.md` in this directory.

**Live, viewable/presentable version**: https://claude.ai/artifact/LxdYuYeQXxHDsX18KWxBLA

**What's here**: `deck.json` (the slide order/outline) and `slides/*.html` —
the raw content of each slide, in the closed HTML/CSS subset the Claude
Artifact "Slides" type uses. Each slide file is a bare `<section id="...">`
fragment (no `<html>`/`<head>`/`<body>`, no linked stylesheet) — **opening
one directly in a browser will not render it correctly**. This is a content
record for diffing/reviewing/reusing the text and numbers, not a
standalone-viewable format. Use the live link above to actually view or
present it.

If the live deck is edited later, this filesystem copy will not update
automatically — treat it as a snapshot as of 2026-09-21.
